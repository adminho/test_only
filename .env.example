# Server
PORT=3000

# Absolute path to the upload directory (must be outside web root)
# Default resolves to <project-root>/uploads at runtime
UPLOAD_DIR=

# Maximum allowed upload size in bytes (default: 10 MB)
MAX_FILE_SIZE=10485760
