# Server
PORT=3000
BASE_URL=http://localhost:3000

# Storage — absolute or relative path for uploaded PDFs
UPLOAD_DIR=./uploads

# Database — JSON store file path
DB_PATH=./data/uploads.json

# Max upload size in megabytes (default: 10)
MAX_FILE_SIZE_MB=10
