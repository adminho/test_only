{
  "name": "upload-api",
  "version": "1.0.0",
  "description": "File upload API with size validation",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "jest --testEnvironment=node"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
