{
  "name": "file-upload-service",
  "version": "1.0.0",
  "description": "File upload service with filename sanitization",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "test": "node --test tests/"
  },
  "dependencies": {
    "express": "^4.18.2",
    "multer": "^2.1.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
