{
  "name": "pdf-upload-api",
  "version": "1.0.0",
  "description": "PDF upload API with validation, secure storage, and metadata persistence",
  "main": "src/app.js",
  "scripts": {
    "start": "node src/app.js",
    "dev": "node --watch src/app.js",
    "test": "jest --runInBand --forceExit"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.1.1",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "jest": {
    "testEnvironment": "node"
  }
}
